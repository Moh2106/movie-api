spring:
  application:
    name: movie-api

  datasource:
    username: postgres
    password: 12345
    url: jdbc:postgresql://localhost:5432/db_movie
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


server:
  port: 9090

project:
  poster: poster/
